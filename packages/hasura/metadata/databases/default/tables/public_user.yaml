table:
  schema: public
  name: user
array_relationships:
  - name: group_members
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          schema: public
          name: group_member
  - name: groups
    using:
      foreign_key_constraint_on:
        column: created_by
        table:
          schema: public
          name: group
